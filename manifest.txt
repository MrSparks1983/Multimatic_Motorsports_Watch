VERSION=v1.6.17
BIN_URL=https://raw.githubusercontent.com/MrSparks1983/Multimatic_Motorsports_Watch/firmware/Multimatic_Motorsports_Watch_v1_6_17.bin
NOTES=USB screen-off stability hotfix: prevents repeated rebooting when the display timeout occurs while USB/VBUS is connected. While externally powered the watch now uses a zero-brightness pseudo-off state instead of issuing the AMOLED hardware displayOff command; the display controller remains initialized and brightness is restored directly on wake. Battery-powered screen-off behavior remains unchanged. Retains OTA Wi-Fi handoff, Settings scroll hard-limit, touch ownership, Wi-Fi/BLE connectivity and remote OTA support.
MD5=
