VERSION=v1.8.7
BIN_URL=https://raw.githubusercontent.com/MrSparks1983/Multimatic_Motorsports_Watch/firmware/Multimatic_Motorsports_Watch_v1_8_7_UI.ino.bin
NOTES=OTA workflow automation based directly on v1.8.6. CHECK VIA WI-FI now reboots into the low-memory Wi-Fi updater and automatically resumes Settings > Firmware, connects to the saved network and checks GitHub as soon as Wi-Fi is stable; no second navigation or CHECK AGAIN tap is required. Startup PIN unlock preserves the Firmware page during updater/resume boots. Successful direct Wi-Fi, Windows BLE or browser OTA stores a one-shot post-update resume flag so the newly installed firmware automatically opens Firmware after reboot and shows UPDATE COMPLETE with the installed version. Retains the v1.8.6 larger Crew popup, v1.8.5 production UI cleanup, PSRAM framebuffer, TLS recovery, telemetry, sessions, alerts, Crew messaging, Wi-Fi reconnect and resolved scrolling.
MD5=
