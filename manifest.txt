VERSION=v1.6.21
BIN_URL=https://raw.githubusercontent.com/MrSparks1983/Multimatic_Motorsports_Watch/firmware/Multimatic_Motorsports_Watch_v1_6_21.bin
NOTES=OTA direct-transfer and Settings bounded-scroll update. After FW:WIFI_HANDOFF the watch now uses direct WiFiClientSecure transport for both the GitHub manifest and firmware BIN instead of returning to HTTPClient for the firmware download. OTA requires an exact Content-Length, writes the inactive OTA partition, retains MD5 verification, verifies the embedded PROWATCH_FW_VERSION marker, activates the new partition and performs one controlled reboot. Failed OTA restores BLE advertising. Settings raw touch scrolling now uses LVGL bounded scrolling with no animated finger-release snap, preventing the menu from continuing beyond its real content boundaries. Retains USB screen-off stability, BLE handoff lock, canonical firmware version reporting and remote OTA support.
MD5=
